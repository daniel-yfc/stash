# CK-Download
# Private build: cookies + CookieURL. Do not set useCDP in entry points.
# Search verified 2026-08-27 from rendered /product/result?kw=雄穴
# (ul.title_list cards: /product/detail/34124, 34114, 27341).
# Detail sceneScraper UNVERIFIED on guest pages — product table is login-gated.
# Last Updated: 2026-08-28

name: CK-Download

sceneByURL:
  - action: scrapeXPath
    url:
      - ck-download.com/product/detail/
      - www.ck-download.com/product/detail/
    scraper: sceneScraper

sceneByName:
  action: scrapeXPath
  queryURL: "https://www.ck-download.com/product/result?kw_opt=1&kw={}&st_opt=1&p_opt=1&mt_opt=1"
  scraper: searchScraper

sceneByQueryFragment:
  action: scrapeXPath
  queryURL: "{url}"
  scraper: sceneScraper

sceneByFragment:
  action: scrapeXPath
  queryURL: "https://www.ck-download.com/product/result?kw_opt=1&kw={title}&st_opt=1&p_opt=1&mt_opt=1"
  queryURLReplace:
    title:
      - regex: '\.[^.]+$'
        with: ''
      - regex: '[_\.]+'
        with: ' '
      - regex: '\s{2,}'
        with: ' '
      - regex: '^\s+|\s+$'
        with: ''
  scraper: searchScraper

xPathScrapers:
  searchScraper:
    common:
      $results: "//ul[contains(@class,'title_list')]//li[.//a[contains(@href,'/product/detail/')]]"
    scene:
      Title:
        selector: "$results//h4[1]"
        postProcess:
          - replace:
              - regex: "^\\s+|\\s+$"
                with: ""
      URLs:
        selector: "$results//a[contains(@href,'/product/detail/')][1]/@href"
        postProcess:
          - javascript: |
              var v = value || "";
              if (!v) return "";
              v = v.replace(/^\/\//, "https://");
              if (v.indexOf("http") === 0) return v;
              if (v.indexOf("/") === 0) return "https://www.ck-download.com" + v;
              return "https://www.ck-download.com/" + v;
      Image:
        selector: "$results//div[contains(@class,'photo')]//img[1]/@src"
        postProcess:
          - javascript: |
              var v = value || "";
              if (!v) return "";
              v = v.replace(/^\/\//, "https://");
              if (v.indexOf("http") === 0) return v;
              if (v.indexOf("/") === 0) return "https://www.ck-download.com" + v;
              return "https://www.ck-download.com/" + v;
      Date:
        selector: "$results//div[contains(@class,'date')][1]"
        postProcess:
          - replace:
              - regex: "(\\d{4})[./-](\\d{1,2})[./-](\\d{1,2})"
                with: "$1-$2-$3"
          - parseDate: "2006-01-02"

  sceneScraper:
    common:
      $data: "//table[contains(@class,'prod_data')][1]"
      $cats: "//div[contains(@class,'prod_category')][1]"
    scene:
      Title:
        selector: "$data//th[normalize-space(.)='DVD タイトル']/following-sibling::td[1] | //h1[normalize-space()] | //title"
        postProcess:
          - replace:
              - regex: "^\\s+|\\s+$"
                with: ""
      Code:
        selector: "$data//th[normalize-space(.)='プロダクトナンバー']/following-sibling::td[1]"
        postProcess:
          - replace:
              - regex: "^\\s+|\\s+$"
                with: ""
      Date:
        selector: "//div[contains(@class,'add_info')]//div[contains(@class,'date')][1]"
        postProcess:
          - javascript: |
              var v = value || "";
              if (!v) return "";
              var cleaned = v.replace(/\s*UP.*$/iu, "").trim();
              var m = cleaned.match(/(\d{4})[.\-\/](\d{1,2})[.\-\/](\d{1,2})/u);
              if (m) {
                var mm = ("0" + m[2]).slice(-2);
                var dd = ("0" + m[3]).slice(-2);
                return m[1] + "-" + mm + "-" + dd;
              }
              return cleaned;
          - parseDate: "2006-01-02"
      Studio:
        Name:
          selector: "$data//th[normalize-space(.)='レーベル']/following-sibling::td[1]//a[1]"
          postProcess:
            - replace:
                - regex: "^\\s+|\\s+$"
                  with: ""
      Tags:
        Name:
          selector: "$cats//li[strong[normalize-space(.)='プレイ内容']]//a | $cats//li[strong[normalize-space(.)='モデルタイプ']]//a"
          postProcess:
            - replace:
                - regex: "^\\s+|\\s+$"
                  with: ""
      Details:
        selector: "//div[contains(@class,'intro_text')]//p[string-length(normalize-space(.)) > 10 and not(contains(@style,'color'))]//text()[normalize-space()]"
        concat: "\n\n"
      Image:
        selector: "//div[contains(@class,'photo_flexslider')]//img[not(contains(@src,'no_image'))][1]/@src | //meta[@property='og:image']/@content"
        postProcess:
          - javascript: |
              var v = value || "";
              if (!v) return "";
              v = v.replace(/^\/\//, "https://");
              if (v.indexOf("http") === 0) return v;
              if (v.indexOf("/") === 0) return "https://www.ck-download.com" + v;
              return "https://www.ck-download.com/" + v;

driver:
  headers:
    - Key: User-Agent
      Value: Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:133.0) Gecko/20100101 Firefox/133.0
    - Key: Accept-Language
      Value: ja,en-US;q=0.8,en;q=0.6
  cookies:
    - CookieURL: "https://www.ck-download.com"
      Cookies:
        - Name: USER_AUTO_LOGIN
          Value: Q2FrZQ%3D%3D.NGNmYWE0OTcxNTI2NTg2OGNmOGI3M2I4NGEwMTJmOGEzOGFhNzZjZDM4MjdlOTQ0NGEzOGY4ZjY0ZGExNWY3ZOhMjOwJKWi7PfbEzqNSR1JXHEoRUyraLjCRIjdB8gprjNpsbcM8%2BjWeL%2BV6ONrbXEcyh8Lt1HY7MAmAyytdbTV%2BbJmLTUP46isqFD3rNgIDRrGbdiJgBCkkcJb%2FvLYi3w%3D%3D
          Domain: www.ck-download.com
          Path: "/"
        - Name: COAT
          Value: tb22q2vor6ie381kogs6483b18
          Domain: www.ck-download.com
          Path: "/"
