# Stash Scraper Builder Skill — Review Notes

**Reviewer:** Perplexity (external AI review agent)
**Scope:** `skills/stash-scraper-builder/` and `references/` vs. 9 official Stash/CommunityScrapers documentation sources
**Date:** 2026-08-30

## Sources checked

1. https://docs.stashapp.cc/in-app-manual/scraping/
2. https://docs.stashapp.cc/in-app-manual/scraping/scraperdevelopment/
3. https://docs.stashapp.cc/metadata-sources/
4. https://docs.stashapp.cc/api/
5. https://docs.stashapp.cc/guides/scraping-metadata-behind-login/
6. https://docs.stashapp.cc/metadata-sources/scrapers/
7. https://deepwiki.com/stashapp/CommunityScrapers/8.4-driver-configuration
8. https://deepwiki.com/stashapp/CommunityScrapers/5.2-api-authentication
9. https://deepwiki.com/stashapp/CommunityScrapers/1.1-system-architecture

## Part 1: Material Gaps and Risk

| # | Domain | Observation | Recommendation |
|---|---|---|---|
| 1 | Scope vs. referenced docs | The skill's reference list implies coverage of `docs.stashapp.cc/api/` and `metadata-sources/`, but the repo has zero hits for `stashServer`, `ApiKey`, `graphql`, or `stash-box`. Only `action: scrapeXPath`, `action: scrapeJson`, and `action: script` are implemented. | Add an explicit "Out of scope" section to `SKILL.md` (or a new `references/out-of-scope.md`) stating: no `action: stash`, no stash-box integration, no Stash GraphQL API usage. Keep the API/metadata-sources links as background only. |
| 2 | Entry-point completeness | `sceneByFragment` is enforced via CI (Rule 4) and present in all scrapers; `sceneByQueryFragment` correctly pairs with `sceneByName`. However, performer/group entry points (`performerByURL/ByName/ByFragment`, `groupByURL`) exist only in schema files, with no worked example in any actual scraper or `examples.md`. | Add at least one minimal example YAML block for `performerByFragment` in `examples.md` so schema support and practical guidance match. |

## Part 2: Operational Optimization

| # | Domain | Observation | Recommendation |
|---|---|---|---|
| 3 | CDP / cookie security posture | Public scrapers correctly forbid `driver.cookies`, using `useCDP: true` + visible-Chrome login; `CookieURL` mutual-exclusivity rules are documented in `schema-checklist.md` / `cdp-workflow.md`. Private scrapers use cookies + `CookieURL` as intended. | Add a one-line checklist item to `schema-checklist.md`: "If `useCDP: true`, remove all `CookieURL` entries; if `useCDP: false` and using cookies, every cookie must have `CookieURL`." |
| 4 | Date parsing & postProcess ordering | `date-formats.md` / `post-processing.md` correctly document Go-style `parseDate` layouts and `replace`-before-`parseDate` ordering; real scrapers follow this. | Optional: add a "common failure" example (broken layout vs. fixed) to `date-formats.md`, since this is a frequent source of silent empty-date bugs. |
| 5 | Advanced patterns (subScraper) | `advanced-patterns.md` is referenced for `subScraper`, anchors, networks, studio map, but `subScraper` has zero usage in any actual scraper — anchors and studio `map` are used, `subScraper` is not illustrated anywhere. | Clarify whether `subScraper` is deprecated/discouraged for this skill, or add one minimal working example. |
| 6 | queryURL / queryURLReplace correctness | `json-patterns.md` correctly states official placeholders (`{}`, `{url}`, `{filename}`) and that `sceneByName` must not use `queryURLReplace`. All search scrapers implement this correctly. | No change needed — strength of the repo. |
| 7 | Last Updated header & CI enforcement | `# Last Updated: YYYY-MM-DD` is enforced by `scraper-quality-gate.sh` (Rule 5) and present in every public/private scraper file. | No change needed — strength of the repo. |
| 8 | API authentication patterns | DeepWiki 5.2 covers third-party API auth (Algolia, ASGMax, etc.) but the repo has no equivalent Authorization-header/API-key example, consistent with its actual scraper set (Japanese adult sites) but a gap vs. the referenced doc. | If skill stays XPath/JSON-focused, state so explicitly; otherwise add a minimal API-auth example to `json-patterns.md` or `script-actions.md`. |

## zh-TW 快速摘要

| 重點 | 一句話說明 |
|---|---|
| 最大缺口 | 參考文件涵蓋 Stash API 與 stash-box，但技能完全未實作；應在 `SKILL.md` 明確標註「超出範圍」。 |
| 安全與 CDP | 公開爬蟲不用 `driver.cookies`，用 `useCDP: true` + 可見 Chrome 登入；規則已落實，建議在檢核表加一句 CDP/CookieURL 互斥規則。 |
| 入口點完整性 | `sceneByFragment` 已強制；但 performer/group 入口點只有 schema、無範例，建議補一個最小 performer 範例。 |
| 日期與後處理 | `parseDate` / `replace` / `concat` 順序正確，實務也一致；可加常見錯誤範例強化文件。 |
| 進階模式 | `subScraper` 被參考但無實作範例；應說明是否棄用或補一個最小範例。 |
| 整體評價 | 對 CommunityScrapers 風格的 XPath/JSON/script 爬蟲覆蓋完整；對 Stash API / stash-box 是刻意不碰，需文件化此邊界。 |

---
*Generated by Perplexity AI review agent based on GitHub code search against `daniel-yfc/stash` and live fetches of the 9 listed documentation sources.*
