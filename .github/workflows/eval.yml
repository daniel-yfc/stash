name: eval

on:
  workflow_dispatch:  # Changed from push/PR to manual trigger per roadmap #3 / #4
  # push:
  #   paths:
  #     - 'skills/stash-scraper-builder/**'
  # pull_request:
  #   paths:
  #     - 'skills/stash-scraper-builder/**'

jobs:
  eval:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run eval
        run: ./eval-run.sh
        shell: bash
