{
  "name": "stash-scraper-builder",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node validator/index.mjs -a --ci",
    "validate-sort": "node validator/index.mjs -a -s --ci",
    "test": "echo \"No JS tests\" && exit 0"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^3.0.1",
    "better-ajv-errors": "^2.0.3",
    "chalk": "^5.3.0",
    "jsdom": "^30.0.1",
    "xpath": "^0.0.34",
    "yaml": "^2.5.0"
  }
}
